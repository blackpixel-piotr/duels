@* Landscape-everywhere mandate (backlog resolution batch 1 §10): every
   non-combat screen is landscape-only. Combat already handles portrait via
   its own CSS auto-rotate trick (.battle-fs) — this is the OPPOSITE
   approach for everything else: block interaction and ask the player to
   physically rotate, rather than building a second portrait layout for
   every screen. Pure CSS visibility (@media orientation:portrait) — no
   JS/interop needed. *@
<div class="rotate-overlay">
    <div class="rotate-overlay-icon">⟲</div>
    <p class="rotate-overlay-text">Rotate your device to landscape</p>
</div>
